# AI Chatbot Web App

## Overview
This project is an AI chatbot web application that uses the Hugging Face API.

## Requirements
- Flask
- Transformers
- TensorFlow
- Google Cloud AI Platform
- 
## Installation
To install the required packages, run:
```bash
pip install -r requirements.txt
#running the application execute: python main.py (in the ternimal)

